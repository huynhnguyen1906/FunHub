{
	"name": "funhub",
	"version": "1.0.0",
	"description": "Build the social network project",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "concurrently \"npm run server\" \"npm run client\"",
		"server": "cd server && nodemon server.js",
		"client": "cd client && npm run dev"
	},
	"keywords": [
		"React",
		"Express",
		"social",
		"network"
	],
	"author": "Huynhnguyen19006",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.2.2",
		"nodemon": "^3.0.3"
	},
	"dependencies": {
		"dotenv": "^16.3.2"
	}
}
